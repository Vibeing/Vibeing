<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingHorizontal="8dp">

    <TextView
        android:id="@+id/dropdownTxt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:includeFontPadding="false"
        android:textColor="@color/black_white"
        app:layout_constraintBottom_toBottomOf="@+id/dropdownImg"
        app:layout_constraintStart_toEndOf="@+id/dropdownImg"
        app:layout_constraintTop_toTopOf="@+id/dropdownImg"
        tools:text="TextView" />

    <ImageView
        android:id="@+id/dropdownImg"
        android:layout_width="@dimen/_16sdp"
        android:layout_height="@dimen/_16sdp"
        android:layout_marginBottom="4dp"
        android:contentDescription="@string/visibilityImage"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:srcCompat="@drawable/ic_everyone" />
</androidx.constraintlayout.widget.ConstraintLayout>